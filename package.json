{
  "name": "monorepo",
  "author": "Gabriel Duarte (https://gabrielduarte.dev)",
  "private": true,
  "scripts": {
    "test": "ls ./fixtures | xargs -I {} bash -c \"cd ./fixtures/'{}' && yarn run ci\"",
    "ci": "yarn run test"
  },
  "workspaces": [
    "packages/*",
    "fixtures/*"
  ],
  "devDependencies": {
    "@gabrielduartem/eslint-config": "0.0.0-development",
    "lerna": "3.20.2",
    "prettier": "2.0.1"
  }
}
